# /etc/default/lizardfs-chunkserver

# Start lizardfs-chunkserver from init.d script on boot.
#   Only allowed values are "true" and "false".
#   Undefined values default to "false".
LIZARDFSCHUNKSERVER_ENABLE={{ lizardfs.chunkserver.enable_service }}

DAEMON_OPTS=""
