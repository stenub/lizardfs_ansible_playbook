# /etc/default/lizardfs-master

# Start lizardfs-master from init.d script on boot.
#   Only allowed values are "true" and "false".
#   Undefined values default to "false".
LIZARDFSMASTER_ENABLE={{ enable_service }}

DAEMON_OPTS=""
